$(document).ready(function () {
/*------------------------- Counter -----------------------------------*/
    $("#chart-refresh").click(function () {
        $("#chart-img").toggleClass("rotateIn");
        $("#chart-caption").toggleClass("bounceIn");
        setTimeout(function () {
    $('#chart-img').removeClass('rotateIn');
    $("#chart-caption").removeClass("bounceIn");
    }, 1000);
    });
    $("#clock-refresh").click(function () {
    $("#clock-img").toggleClass("rotateIn");
    $("#clock-caption").toggleClass("bounceIn");
    setTimeout(function () {
    $('#clock-img').removeClass('rotateIn');
    $("#clock-caption").removeClass("bounceIn");
    }, 1000);
    });
    $("#users-refresh").click(function () {
    $("#users-img").toggleClass("rotateIn");
    $("#users-caption").toggleClass("bounceIn");
    setTimeout(function () {
    $('#users-img').removeClass('rotateIn');
    $("#users-caption").removeClass("bounceIn");
    }, 1000);
    });
/*--------------------------- Counter --------------------------------*/
$(document).ready( function() {
// get all the table cells where the class is set to "currency" 
$('td.currency').each(function() {
//loop through the values and assign it to a variable 
    var currency = $(this).html();
//strip the non numeric, negative symbol and decimal point characters
// e.g. Spaces and currency symbols 
    var val = Number(currency.replace(/[^0-9\.-]+/g,""));
// check the value and assign class as necessary 
// (I'm sure this could be done with a switch statement 
    if(val > 0) {
        $(this).addClass('text-success');
    }
    if(val < 0) {
        $(this).addClass('text-danger');
    }
    })
})